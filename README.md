# LockLab

LockLab is a reproducible experimentation framework for logic-locking
defences and attacks.

## Current status

Initial project setup and toolchain detection.

## Development setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
locklab doctor